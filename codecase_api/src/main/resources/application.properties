spring.application.name=codecase_api

server.port=9002

# Niveau de logging
logging.level.root=error
# pour le package
logging.level.fr.swif.codecase_api=info
# pour la BDD
logging.level.org.springframework.data=INFO
logging.level.org.springframework.jdbc.core.JdbcTemplate=DEBUG
# Niveau de logging pour TOMCAT
logging.level.org.springframework.boot.web.embedded.tomcat=INFO

# Pour que les logs soient enregistrés dans un fichier
logging.file.name=logs/codecase-api.log

spring.datasource.url=${MYSQL_URL:jdbc:mysql://localhost:3306/codecase_stage}
spring.datasource.username=${MYSQL_USER:Alexandre}
spring.datasource.password=${MYSQL_PASSWORD:Mariemafemme1}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# À LAISSER SOUS NONE
spring.jpa.hibernate.ddl-auto=none

spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

spring.datasource.hikari.initialization-fail-timeout=-1

# Pour la JWT
# A METTRE EN SECRET
app.secret-key=!&%{T;bKtj?GO0|;)_daYEbBpo4!Aj;WKY,LHXEv53t
app.expiration-time=900000